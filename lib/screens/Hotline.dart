import 'package:flutter/material.dart';

class Hotline extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Display hotline"),
    );
  }
}
