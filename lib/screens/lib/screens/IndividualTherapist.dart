import 'package:flutter/material.dart';

class IndividualTherapist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Display individual therapist"),
    );
  }
}
