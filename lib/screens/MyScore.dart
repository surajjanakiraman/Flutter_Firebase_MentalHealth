import 'package:flutter/material.dart';

class MyScore extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("Display Score"),
    );
  }
}
